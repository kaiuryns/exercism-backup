#if !defined(GRAINS_H)
#define GRAINS_H

#include <math.h>
#include <climits>

namespace grains {

    long long unsigned int square(int squareNumber);

    long long unsigned int total();

}  // namespace grains

#endif // GRAINS_H